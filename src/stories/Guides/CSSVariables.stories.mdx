import { Meta, Source, Description } from "@storybook/addon-docs/blocks";
import LinkTo from "@storybook/addon-links/react";
import dedent from "ts-dedent";
import { ThemeVariables } from "../../index";

<Meta title="Guides/CSS Variables" component={ThemeVariables} />

<ThemeVariables />

# CSS Variables

<Description of={ThemeVariables} />

We build our CSS using variables where appropriate, allowing us to support
custom branding and colour schemes such as dark mode and high-contrast.

See the <LinkTo kind="Foundations/Colour" story="page">Colour</LinkTo> and
the <LinkTo kind="Foundations/Typography" story="page">Typography</LinkTo> stories for a
list of CSS variables.

## Example

Render the the `ThemeVariables` component anywhere in your app.

<Source
  language="js"
  code={dedent`
    import { ThemeVariables } from "@thisdotsoftware/react"
    
    <ThemeVariables />
  `}
/>
